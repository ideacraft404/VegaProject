PORT=8080
CONTACT_TO=Swadesh@thevegaforge.com

# Required in production for real email delivery.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM="VEGA Forge <no-reply@vegaforge.in>"

# Local development only. Saves submissions to .submissions when SMTP is not configured.
VEGA_DEV_OUTBOX=true
